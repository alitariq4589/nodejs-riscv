name: Build Nodejs for RISC-V with OpenSSL ASM

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 1 * *"
  push:
    branches:
      - openssl-support

jobs:
  build_and_release:
    runs-on: RISCV64
    permissions:
      contents: write
    outputs:
      tag_name: ${{ steps.get-latest-tag.outputs.tag_name }}
      release_name: ${{ steps.get-version.outputs.release_name }}
      asset_name: ${{ steps.get-version.outputs.asset_name }}
      asset_path: ${{ steps.get-version.outputs.asset_path }}

    steps:
      - name: Install dependencies
        run: |
          #!/bin/bash
          set -eux
          sudo apt-get update
          
          # Install all necessary packages including Perl for OpenSSL
          sudo apt-get install -y libatomic1 libtsan2 gcc-14 g++-14 rustup cmake \
                                  build-essential libcurl4-openssl-dev wget ccache \
                                  python3 python3-pip file perl
          
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
          sudo ln -sf /usr/bin/gcc-14 /usr/bin/gcc
          sudo ln -sf /usr/bin/g++-14 /usr/bin/g++
          
          rustup install stable
          rustup default stable
          
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Directories
        run: mkdir -p $GITHUB_WORKSPACE/installed_binaries

      - name: Clone Node.js and checkout latest tag
        id: get-latest-tag
        run: |
          set -eux

          git config --global http.postBuffer 524288000
          git config --global http.maxRequests 2
          git config --global core.compression 0

          rm -rf node
          git clone --depth=1 --no-tags https://github.com/nodejs/node.git
          cd node

          git fetch --tags --depth=1

          LATEST_TAG=$(git tag -l "v*" | sort -V | tail -n 1)
          echo "Checking out $LATEST_TAG"

          git fetch --depth=1 origin tag $LATEST_TAG
          git checkout FETCH_HEAD

          echo "tag_name=$LATEST_TAG" >> "$GITHUB_OUTPUT"

      - name: Inspect OpenSSL Config System
        run: |
          cd node
          
          echo "=== Inspecting OpenSSL Configuration System ==="
          
          # Check what architectures are available
          echo "Available OpenSSL architectures:"
          ls -la deps/openssl/config/archs/ 2>/dev/null || echo "No archs directory found"
          
          # Check if there's a config generation script
          echo -e "\nLooking for OpenSSL config generation tools..."
          find deps/openssl -name "*.py" -o -name "gen*" -o -name "Makefile" | grep -i config || true
          
          # Check the main OpenSSL Configure script
          if [ -f "deps/openssl/openssl/Configure" ]; then
            echo -e "\nChecking OpenSSL Configure for RISC-V support..."
            grep -i "riscv" deps/openssl/openssl/Configure || echo "No RISC-V mention found"
          fi

      - name: Generate RISC-V OpenSSL Configuration (Fixed)
        run: |
            cd node/deps/openssl

            echo "=== Generating RISC-V OpenSSL Configuration ==="

            # The directory exists but only has no-asm, we need to populate the asm variant
            cd openssl

            # Clean any previous state
            git clean -fdx 2>/dev/null || true

            # Configure OpenSSL directly for RISC-V
            ./Configure linux64-riscv64 \
              no-shared \
              no-tests \
              --prefix=/tmp/openssl-riscv64

            # Check if Configure succeeded
            if [ -f "configdata.pm" ]; then
              echo "✅ OpenSSL configured successfully for RISC-V"

              # Create the directory structure
              mkdir -p ../config/archs/linux64-riscv64/asm/include/openssl
              mkdir -p ../config/archs/linux64-riscv64/asm/include/crypto
              mkdir -p ../config/archs/linux64-riscv64/asm/crypto/include/internal
              mkdir -p ../config/archs/linux64-riscv64/asm/providers/common/include

              # Copy generated headers
              cp -v include/openssl/configuration.h ../config/archs/linux64-riscv64/asm/include/openssl/ 2>/dev/null || true
              cp -v include/openssl/opensslconf.h ../config/archs/linux64-riscv64/asm/include/openssl/ 2>/dev/null || true
              cp -v include/crypto/bn_conf.h ../config/archs/linux64-riscv64/asm/include/crypto/ 2>/dev/null || true
              cp -v include/crypto/dso_conf.h ../config/archs/linux64-riscv64/asm/include/crypto/ 2>/dev/null || true

              # Generate buildinf.h
              cat > ../config/archs/linux64-riscv64/asm/crypto/buildinf.h << 'EOF'
              #define PLATFORM "platform: linux64-riscv64"
              #define DATE "built on: reproducible build, date unspecified"
              EOF

              # Copy internal headers
              cp -v crypto/include/internal/*.h ../config/archs/linux64-riscv64/asm/crypto/include/internal/ 2>/dev/null || true

              # Create empty asm directories to satisfy build system
              mkdir -p ../config/archs/linux64-riscv64/asm/crypto
              touch ../config/archs/linux64-riscv64/asm/crypto/.gitkeep

              echo "✅ RISC-V OpenSSL ASM configuration created"
            else
              echo "❌ OpenSSL Configure failed, will fall back to no-asm"
            fi

            cd ../../..

            # Verify the structure
            echo "=== RISC-V Config Structure ==="
            ls -la deps/openssl/config/archs/linux64-riscv64/

      - name: Configure Node.js  
        run: |
          cd node
          # Check what was created
          if [ -d "deps/openssl/config/archs/linux64-riscv64/asm" ] && \
             [ -f "deps/openssl/config/archs/linux64-riscv64/asm/include/openssl/configuration.h" ]; then
            echo "🚀 RISC-V OpenSSL ASM config ready"
            echo "Configuring Node.js with ASM optimizations..."
            # Don't use --openssl-arch flag (doesn't exist)
            # Just configure normally - it will auto-detect RISC-V
            ./configure --prefix=$(readlink -f $GITHUB_WORKSPACE/installed_binaries) \
                        --dest-cpu=riscv64
          else
            echo "⚠️ RISC-V OpenSSL ASM config incomplete, using no-asm"
            ./configure --prefix=$(readlink -f $GITHUB_WORKSPACE/installed_binaries) \
                        --dest-cpu=riscv64 \
                        --openssl-no-asm
          fi
          # Show what OpenSSL config was selected
          echo "=== OpenSSL Configuration Selected ==="
          grep -i "openssl" config.gypi | head -20

      - name: Build
        run: |
          cd node
          make -j$(nproc)

      - name: Install
        run: |
          cd node
          make install

      - name: Verify installation
        run: |
          cd $GITHUB_WORKSPACE/installed_binaries
          ./bin/node -v
          file ./bin/node
          
          # Check if OpenSSL is linked properly
          ldd ./bin/node | grep -i ssl || echo "OpenSSL statically linked (expected)"

      - name: Get Node.js version and set outputs
        id: get-version
        run: |
          NODE_VERSION=$(cd $GITHUB_WORKSPACE/installed_binaries/bin && ./node -v | cut -c 2-)
          ASSET_NAME="nodejs-${NODE_VERSION}-riscv64-linux.tar.gz"
          ASSET_PATH="$GITHUB_WORKSPACE/${ASSET_NAME}"

          echo "release_name=Node.js ${NODE_VERSION} RISC-V" >> $GITHUB_OUTPUT
          echo "asset_name=$ASSET_NAME" >> $GITHUB_OUTPUT
          echo "asset_path=$ASSET_PATH" >> $GITHUB_OUTPUT

      - name: Package binaries
        run: |
          tar -czf ${{ steps.get-version.outputs.asset_path }} -C ${{ github.workspace }}/installed_binaries .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.get-version.outputs.asset_name }}
          path: ${{ steps.get-version.outputs.asset_path }}

      - name: Create or push to tag
        run: |
          TAG=${{ steps.get-latest-tag.outputs.tag_name }}

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if git ls-remote --exit-code --tags origin "refs/tags/$TAG" >/dev/null 2>&1; then
            echo "✅ Tag $TAG already exists. Skipping tag creation." 
          else
            echo "🆕 Tag $TAG does not exist. Creating and pushing..."
            git tag "$TAG"
            git push origin "$TAG"
          fi

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.get-latest-tag.outputs.tag_name }}
          files: ${{ steps.get-version.outputs.asset_path }}